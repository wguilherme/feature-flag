import type { EvaluationContext, Logger, JsonValue } from '@openfeature/core';
export declare class Targeting {
    private logger;
    private readonly _logicEngine;
    constructor(logic: unknown, logger: Logger);
    evaluate<T extends JsonValue>(flagKey: string, ctx: EvaluationContext, logger?: Logger): T;
}
